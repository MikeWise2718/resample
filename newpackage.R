library(utils)
require(reaferg)
detach(package:resample)
remove.packages(c("resample"))

#curwd <- getwd()
#setwd("f:/88acres/5-packages/")
install.packages("resample",type="source",repo=NULL,verbose=T)
#setwd(curwd)
